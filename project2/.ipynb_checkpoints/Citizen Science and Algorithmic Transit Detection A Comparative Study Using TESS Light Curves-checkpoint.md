# Citizen Science and Algorithmic Transit Detection A Comparative Study Using TESS Light Curves

## Abstract

This paper investigates how human visual judgment and an automated Box Least Squares algorithm complement each other in detecting exoplanet transits in TESS light curves. I developed an interactive Python tool that presents raw light curves from multiple sectors, allows a human to select files for detailed analysis, and then runs a stepwise pipeline consisting of sigma clipping, median filter detrending, BLS period search, phase folding, and transit marking. The study documents cases where both human and algorithm agree, where the algorithm finds subtle signals invisible to the eye, and where both fail, and it emphasizes that humans contribute uniquely by recognizing when an algorithmic detection is not physically meaningful. The paper leaves space for the program outputs and figures to be inserted.

## Introduction

Exoplanet transit detection relies on identifying periodic decreases in stellar brightness caused by a planet crossing the stellar disk, and automated methods such as Box Least Squares are widely used because they efficiently search for box shaped dips across large data sets. The raw data are discrete samples of stellar flux ( f(t_i) ) measured at times ( t_i ), and real observations include instrumental noise, cosmic ray events, and intrinsic stellar variability that can obscure or mimic transit signals. To mitigate these effects I first remove extreme outliers using sigma clipping, which flags points satisfying (|f(t_i)-\mu|>k\sigma) with (k=5) in this implementation so that isolated spikes do not bias subsequent steps. I then estimate a low frequency trend (T(t)) using a median filter and form a normalized light curve (F(t)=f(t)/T(t)) to isolate short duration dips. The BLS algorithm evaluates a grid of trial periods (P) and durations (\tau) by folding the light curve modulo each (P) and fitting a box model (M(\phi)) where (\phi=((t-t_0)/P)\bmod 1) and the model takes the value (1-\delta) inside the transit window and (1) outside, and the detection statistic is proportional to the variance reduction achieved by the box fit. Phase folding with the best period aligns candidate transits so that repeated dips appear at the same phase if the detection is physically meaningful. The mathematical structure of these steps also explains common failure modes: sigma clipping can remove real short events if thresholds are too aggressive, median filtering can distort or remove long shallow transits if the kernel is poorly chosen, and BLS will always return a best period even when the data are multi periodic or dominated by noise, which can produce a high detection statistic for a mathematically consistent but physically spurious box model. These failure modes motivate the human role of model validation, because humans can often recognize when a phase folded signal is scattered across phases or otherwise inconsistent with a stable orbital transit.

## Methods

I used TESS light curves retrieved from the MAST archive and implemented an interactive analysis tool in Python that queries available timeseries products for a given TIC, displays each raw light curve sequentially for human inspection, and then allows the user to select one or more files for detailed processing. The processing pipeline follows the mathematical steps described in the introduction and is implemented as follows. The program reads the time series and selects the flux column preferring PDCSAP flux when available, then removes non finite values and sorts by time. Sigma clipping is applied to the flux series to mask outliers, and a median filter with an odd kernel size is used to estimate the low frequency trend; the flattened flux is computed by dividing by the trend and replacing zeros in the trend with a robust median to avoid division artifacts. The BLS search is performed on a dense grid of trial periods (P) in a chosen range and a set of trial durations (\tau), and the best period is selected as the one that maximizes the BLS power. Phase folding uses the best period and transit epoch (t_0) to compute phase (\phi) and produce a folded light curve for visual inspection. For reproducibility the program logs the file identifiers, time ranges, number of points, and the BLS summary statistics including best period, transit duration, and detection power. The interactive design intentionally shows raw curves first so that the human can exercise pattern validity checks before the algorithmic output is emphasized.

## Results

Below I present the categories of outcomes observed during analysis and leave placeholders for the program outputs and figures that illustrate each case. Insert the program figures and numerical summaries where indicated.

Both human and algorithm correct. In these examples the raw light curve shows a clear, repeating dip that survives sigma clipping and detrending, the BLS periodogram displays a sharp peak at a single period, and the phase folded light curve shows a coherent transit shape with consistent depth and duration. Insert figures and numerical results for one or more clean detections here.

Algorithm correct but human misses. In these examples the transit depth is shallow or the noise level is high so that the dip is not obvious by eye, yet the BLS algorithm finds a period with significant power and the phase folded curve reveals a consistent dip when binned or when the transit mask is applied. Insert figures and numerical results for one or more subtle detections here.

Both human and algorithm wrong. These examples include multi periodic signals and pure noise cases that illustrate why both approaches can fail. For multi periodic light curves the BLS algorithm, constrained to a single period model, often returns a period that maximizes the variance reduction but produces a phase folded diagram where the candidate dips are scattered across phases rather than forming a stable transit. For noise dominated light curves the algorithm still returns a best period because the box model can fit random fluctuations in a way that reduces variance, but the folded light curve lacks coherent structure. Insert figures and numerical results for representative multi periodic and noise dominated cases here.

A concise table summarizing the files analyzed, the best BLS period and power for each file, the human judgment for each file, and a short note on the outcome is useful for the report. Insert the summary table here.

## Discussion

The mathematical steps of the pipeline clarify why the algorithm can fail and why human judgment is valuable. Sigma clipping removes extreme values but can also remove legitimate short events if thresholds are too strict, and median filter detrending removes low frequency variability but can distort transit shapes when the filter kernel is comparable to the transit duration. The BLS algorithm is optimal for detecting single periodic box shaped signals in white noise, but its assumption of a single stable period makes it vulnerable to multi periodic systems and to structured noise that mimics periodicity. Because BLS must return a best period, it can be overconfident in cases where the data violate its assumptions. Humans contribute by performing a model validity check: when the phase folded points are scattered across phase or when dips vary in depth and timing, a human can flag the detection as suspicious even without identifying a correct alternative period. This human ability to reject spurious algorithmic outputs reduces false positives and focuses follow up on candidates that are both algorithmically significant and visually plausible. The interactive tool supports this workflow by presenting raw curves first, which encourages the human to apply pattern validity checks before being influenced by the algorithmic result.

## Conclusion

The interactive analysis demonstrates that humans and algorithms play complementary roles in exoplanet transit detection, with algorithms excelling at finding subtle periodic signals and humans excelling at recognizing when algorithmic assumptions break down. The most scientifically valuable human contribution is not always finding the correct period but rather identifying when a reported period is not physically meaningful, such as when phase folded signals are scattered or when the data are dominated by multi periodicity or noise. Future work could extend the pipeline to include multi period search methods, machine learning classifiers trained to recognize model failure, and systematic studies of inter observer agreement to quantify how reliably humans detect algorithmic false positives. Insert any final remarks and next steps here.

### Converting this document to DOCX

To create a DOCX file from this Markdown, save the text above as a `.md` file and convert it locally using a tool such as Pandoc with a command like `pandoc -o paper.docx paper.md`. If you prefer a different style or need a template for the DOCX conversion, I can provide a short Pandoc command line and a minimal YAML metadata header to include title and author information.
